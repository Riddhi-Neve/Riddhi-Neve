#include <stdio.h>
#define max 15
int main() {
	     int max=15;
	         char buf[max];
	    printf("Enter a string: ");
	          gets(buf);
	          printf("String is : %s\n",buf);	    
   return 0;
 }
